{
  "id": "full-bleed",
  "name": "Full Bleed",
  "description": "No persistent navigation. Scroll-driven hero-first layout. Used by portfolio landing pages.",
  "layout": "stack",
  "atoms": "_flex _col",
  "config": {
    "regions": [
      "header",
      "body"
    ],
    "grid": {
      "areas": [
        [
          "header"
        ],
        [
          "body"
        ]
      ]
    },
    "nav": {
      "position": "top",
      "defaultState": "hidden"
    },
    "header": {
      "height": "52px",
      "sticky": true
    },
    "body": {
      "scroll": true
    }
  },
  "configurable": {
    "nav": {
      "positions": [
        "top"
      ],
      "modes": [
        "full",
        "hidden"
      ]
    },
    "header": {
      "toggleable": true
    },
    "sizing": {
      "headerHeight": {
        "min": 36,
        "max": 80,
        "default": 52
      }
    }
  },
  "internal_layout": {
    "root": {
      "display": "flex",
      "direction": "column",
      "atoms": "_flex _col"
    },
    "header": {
      "height": "52px",
      "position": "fixed",
      "top": "0",
      "left": "0",
      "width": "100%",
      "display": "flex",
      "align": "center",
      "justify": "space-between",
      "padding": "0 2rem",
      "z_index": "40",
      "background": "transparent",
      "atoms": "_fixed _top0 _left0 _wfull _flex _aic _jcsb _px8 _py4 _z[40]",
      "left_content": "Brand/logo link",
      "right_content": "Nav links \u2014 flex with gap 1.5rem",
      "note": "Floating nav overlays hero. No border, transparent background.",
      "button_sizing": "Buttons in the header use compact sizing: py-1.5 px-3 text-sm (~32px tall). The header is a tight 52px bar \u2014 default d-interactive padding is too large here."
    },
    "body": {
      "flex": "1",
      "direction": "column",
      "atoms": "_flex _col",
      "note": "Full-width sections stack vertically. Each section is full viewport height or auto. Sections own their own padding. Natural document scroll, no scroll container."
    }
  },
  "guidance": {
    "section_label_treatment": "d-label",
    "section_density": "spacious",
    "shell_spacing": "Full-bleed shell owns floating header offsets and section stack rhythm. Pages should not add extra shell-width wrappers unless a route contract explicitly calls for a local reading measure or content column."
  },
  "code": {
    "imports": "Target-specific imports vary. Translate the internal_layout contract into your framework's floating header, route outlet, and full-bleed section stack.",
    "example": "FullBleedShell(root) -> FloatingHeader(brand, nav links, compact actions) + SectionStack(full-width sections with natural document scroll)"
  },
  "$schema": "https://decantr.ai/schemas/shell.v1.json"
}
