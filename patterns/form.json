{
  "$schema": "https://decantr.ai/schemas/pattern.v2.json",
  "id": "form",
  "version": "1.0.0",
  "decantr_compat": ">=0.9.0",
  "name": "Form",
  "description": "Structured form with labeled field groups, validation states, and action buttons",
  "dependencies": {
    "patterns": {},
    "styles": {},
    "requires_core": {
      "components": [
        "Card",
        "Input",
        "Select",
        "Switch",
        "Checkbox",
        "Button",
        "Label",
        "Textarea",
        "RadioGroup"
      ],
      "charts": []
    }
  },
  "components": [
    "Card",
    "Input",
    "Select",
    "Switch",
    "Checkbox",
    "Button",
    "Label",
    "Textarea",
    "RadioGroup"
  ],
  "default_preset": "settings",
  "presets": {
    "settings": {
      "description": "Vertical stack of sections, each with a title/description on the left and form fields on the right (2-column layout per section). Save button at bottom.",
      "components": [
        "Card",
        "Input",
        "Select",
        "Switch",
        "Button",
        "Label",
        "Textarea"
      ],
      "layout": {
        "layout": "stack",
        "atoms": "_flex _col _gap6 _p4",
        "slots": {
          "section": "Card with 2-column layout: labels left, fields right",
          "section-title": "Section heading with _heading4 and description with _bodysm _fgmuted",
          "field-group": "Grid of form fields with _grid _gc1 _lg:gc2 _gap4",
          "actions": "Bottom-aligned save/cancel buttons"
        }
      }
    },
    "creation": {
      "description": "Single-column wizard-like form with sections as steps. Progress indicator at top, prev/next buttons.",
      "components": [
        "Card",
        "Input",
        "Select",
        "Checkbox",
        "Button",
        "Label",
        "Textarea"
      ],
      "layout": {
        "layout": "stack",
        "atoms": "_flex _col _gap6 _p4 _mw[640px] _mx[auto]",
        "slots": {
          "progress": "Step indicator bar at top showing current/total steps",
          "step-card": "Single Card containing current step fields",
          "step-title": "Step heading with _heading4",
          "field-group": "Vertical stack of form fields with _flex _col _gap4",
          "navigation": "Prev/Next buttons at bottom"
        }
      }
    },
    "structured": {
      "description": "Dense form with fieldsets, collapsible sections, and inline validation. Good for complex data entry.",
      "components": [
        "Card",
        "Input",
        "Select",
        "Checkbox",
        "RadioGroup",
        "Button",
        "Label",
        "Textarea"
      ],
      "layout": {
        "layout": "stack",
        "atoms": "_flex _col _gap4 _p4",
        "slots": {
          "fieldset": "Collapsible Card section with border and legend-style title",
          "field-row": "Horizontal field groups with _grid _gc2 _gap4 for dense layout",
          "inline-validation": "Error text below fields with _textsm _fgdestructive",
          "actions": "Button group with save, cancel, reset"
        }
      }
    }
  },
  "visual_brief": "Well-organized form with clear field grouping under section headings. Each section has a heading in heading4 weight and a muted description line, followed by form fields arranged in a one- or two-column grid. Labels sit above their respective fields (stacked, never side-by-side) in small medium-weight text. Input fields use consistent height (~40px), rounded corners (r2), and border styling that brightens on focus with a primary-color ring. Select dropdowns match input styling with a custom chevron. Textareas have a taller minimum height (6rem). Required fields show a small asterisk. Validation errors display below the field in small destructive-red text. The form is constrained to 640px max-width for readability. Action buttons (Save, Cancel) sit at the bottom right, separated by a top border or spacing from the form fields.",
  "responsive": {
    "mobile": "Single column for all field groups. Fields go full-width. Action buttons stack vertically at full width, primary on top. Section headings go full-width above fields. Padding reduces to p3.",
    "tablet": "Two-column field grid activates for shorter fields (name, email). Textareas span full width. Action buttons stay horizontal, right-aligned.",
    "desktop": "Full two-column grid for field groups. Settings preset shows section label column on the left, fields on the right. Generous p4 spacing. Inline validation visible without layout shift."
  },
  "motion": {
    "micro": {
      "field-focus": "border-color transition 150ms ease-out",
      "button-press": "scale(0.97) 100ms ease-out",
      "error-shake": "translateX(-4px, 4px, -2px, 2px, 0) 300ms ease-out on validation error"
    },
    "transitions": {
      "validation-error": "fade + slideDown 200ms ease-out for error message",
      "success-submit": "fade-out form + fade-in success message 300ms ease-out"
    }
  },
  "accessibility": {
    "role": "form",
    "aria-label": "Form",
    "keyboard": [
      "Tab navigates between fields",
      "Shift+Tab navigates backwards between fields",
      "Enter submits when focus is on submit button",
      "Escape cancels or closes modal forms",
      "Arrow keys navigate within radio groups",
      "Space toggles checkboxes and switches"
    ],
    "announcements": [
      "Validation errors announced on field blur",
      "Required field indicator announced on focus",
      "Success confirmation announced on submit",
      "Field group label announced on section entry"
    ],
    "focus_management": "First invalid field receives focus on failed validation. On successful submit, focus moves to success message or next logical action.",
    "labels": "Every input has an associated <label> element with matching htmlFor/id. Labels are always visible — no placeholder-only fields.",
    "validation": "Error messages are linked to inputs via aria-describedby. Invalid fields use aria-invalid='true'. Error text uses role='alert' for screen reader announcement."
  },
  "layout_hints": {
    "label_position": "stacked",
    "note": "Labels go ABOVE their field, not side-by-side. This prevents the label-field gap problem at wide viewports.",
    "max_width": "Form content should be constrained to max-width: 40rem (640px). Full-width forms are hard to read.",
    "section_grouping": "Group related fields under section headers. Use a SINGLE d-surface card for the entire form, OR no card at all. Do NOT wrap each section in its own separate card.",
    "icon_placement": "Section header icons render INLINE with the heading text (icon left of heading, vertically centered), not floating outside the card border.",
    "select_styling": "Apply d-control to ALL form elements including <select>. Add appearance: none and a custom SVG chevron for consistent styling.",
    "textarea": "Textareas should have min-height: 6rem to visually differentiate from single-line inputs."
  },
  "default_layout": {
    "layout": "stack",
    "atoms": "_flex _col _gap6 _p4",
    "slots": {
      "section": "Card with labeled field groups",
      "section-title": "Section heading with description",
      "field-group": "Grid of form fields",
      "actions": "Save/cancel buttons"
    }
  },
  "io": {
    "consumes": [
      "data"
    ],
    "produces": [
      "form-data"
    ],
    "actions": [
      "save",
      "cancel",
      "reset"
    ]
  },
  "composition": {
    "Form": "Container(d-section, flex-col, gap-6, max-width) > [FormSection[] + ActionButtons]",
    "FormSection": "Card(d-surface) > [SectionTitle(heading4) + SectionDescription?(text-muted) + FieldGroup(d-control, grid: 1/2-col)]",
    "FieldGroup": "Grid > Field[]",
    "Field": "Stack(flex-col) > [Label(d-control, font-medium) + Input(d-control) + ValidationError?(d-annotation, text-destructive)]",
    "ActionButtons": "Row(d-interactive, gap-2) > [SaveButton(variant: primary) + CancelButton(variant: ghost)]"
  }
}
