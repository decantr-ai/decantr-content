{
  "$schema": "https://decantr.ai/schemas/pattern.v2.json",
  "id": "tier-builder",
  "version": "1.0.0",
  "decantr_compat": ">=1.0.0",
  "name": "Tier Builder",
  "description": "Subscription tier configuration interface with drag-reorder, benefit editor, pricing inputs, and live preview.",
  "dependencies": {
    "patterns": {
      "tier-card": "^1.0.0"
    },
    "styles": {},
    "requires_core": {
      "components": [
        "Button",
        "Card",
        "Input",
        "Badge",
        "Switch",
        "icon"
      ],
      "charts": []
    }
  },
  "components": [
    "Button",
    "Card",
    "Input",
    "Badge",
    "Switch",
    "icon"
  ],
  "default_preset": "standard",
  "presets": {
    "standard": {
      "description": "Full tier editor with drag-reorder, benefits, and live preview",
      "components": [
        "Button",
        "Card",
        "Input",
        "Badge",
        "Switch",
        "icon"
      ],
      "layout": {
        "layout": "split",
        "atoms": "_grid _gc1 _lg:gc2 _gap6",
        "slots": {
          "tier-list": "Existing tiers with drag handles for reordering",
          "tier-form": "Create/edit tier form with all fields",
          "benefits-editor": "Add/remove/reorder tier benefits",
          "price-input": "Monthly and yearly pricing inputs",
          "preview": "Live tier card preview"
        }
      }
    },
    "compact": {
      "description": "Simplified tier list view for mobile",
      "components": [
        "Button",
        "Card",
        "Badge",
        "icon"
      ],
      "layout": {
        "layout": "stack",
        "atoms": "_flex _col _gap4",
        "slots": {
          "tier-list": "Compact tier cards",
          "add-button": "Add tier button"
        }
      }
    }
  },
  "default_layout": {
    "layout": "split",
    "atoms": "_grid _gc1 _lg:gc2 _gap6",
    "slots": {
      "tier-list": "Existing tiers",
      "tier-form": "Tier editor form",
      "benefits-editor": "Benefits management",
      "preview": "Live preview"
    }
  },
  "io": {
    "produces": [
      "tier-data",
      "tier-saved"
    ],
    "consumes": [
      "existing-tiers"
    ]
  },
  "visual_brief": "Configuration interface for creating and managing subscription tiers. A tier list on the left shows existing tiers as draggable cards with name, price, and subscriber count. Selecting a tier opens an editor form on the right with fields: tier name input, price input (with currency selector), description textarea, and a benefits editor (sortable list of benefit items with add/remove). A live preview card shows how the tier will appear to subscribers. The compact preset combines the list and editor into a single-column flow. 'Add Tier' button at the bottom of the tier list.",
  "responsive": {
    "mobile": "Single-column layout — tier list at top, editor below when a tier is selected. Preview at the bottom. Drag-to-reorder uses long-press.",
    "tablet": "Two-column layout — tier list on left, editor on right. Preview below the editor.",
    "desktop": "Three-column layout — tier list, editor, and live preview all visible simultaneously."
  }
}
