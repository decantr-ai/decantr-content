{
  "$schema": "https://decantr.ai/schemas/pattern.v2.json",
  "id": "filter-bar",
  "version": "1.0.0",
  "decantr_compat": ">=0.9.0",
  "name": "Filter Bar",
  "description": "Search input and filter controls for filtering page content. Sits above data-consuming patterns like data-table, card-grid, and activity-feed.",
  "dependencies": {
    "patterns": {},
    "styles": {},
    "requires_core": {
      "components": [
        "Input",
        "Select",
        "Button",
        "Badge",
        "icon"
      ],
      "charts": []
    }
  },
  "components": [
    "Input",
    "Select",
    "Button",
    "Badge",
    "icon"
  ],
  "default_preset": "standard",
  "presets": {
    "standard": {
      "description": "Search input + dropdown filters + action buttons in a horizontal bar",
      "components": [
        "Input",
        "Select",
        "Button",
        "icon"
      ],
      "layout": {
        "layout": "row",
        "atoms": "_flex _row _gap3 _aic _w[100%] _py2",
        "slots": {
          "search": "Search Input with placeholder text",
          "filters": "One or more Select dropdowns for category/status filtering",
          "actions": "Action Buttons (clear, apply, etc.)"
        }
      }
    },
    "compact": {
      "description": "Search input only, filters in a collapsible popover",
      "components": [
        "Input",
        "Select",
        "Button",
        "Badge",
        "icon"
      ],
      "layout": {
        "layout": "row",
        "atoms": "_flex _row _gap2 _aic _w[100%] _py1",
        "slots": {
          "search": "Search Input, takes full width",
          "filter-toggle": "Button that opens a Popover with filter Select controls",
          "active-filters": "Badge chips showing currently active filters"
        }
      }
    },
    "advanced": {
      "description": "Multi-row with filter chips, saved filters, and clear-all",
      "components": [
        "Input",
        "Select",
        "Button",
        "Badge",
        "icon"
      ],
      "layout": {
        "layout": "column",
        "atoms": "_flex _col _gap2 _w[100%] _py2",
        "slots": {
          "search-row": "Top row: Search Input + filter dropdowns + action Buttons",
          "chips-row": "Second row: Badge filter chips showing active filters with remove buttons",
          "saved-filters": "Optional row: saved filter presets as outline Buttons"
        }
      }
    }
  },
  "default_layout": {
    "layout": "row",
    "atoms": "_flex _row _gap3 _aic _w[100%] _py2",
    "slots": {
      "search": "Search Input with placeholder text",
      "filters": "One or more Select dropdowns for category/status filtering",
      "actions": "Action Buttons (clear, apply, etc.)"
    }
  },
  "io": {
    "produces": [
      "search",
      "filters",
      "status"
    ],
    "consumes": [],
    "actions": [
      "clear",
      "apply"
    ]
  }
}
