{
  "$schema": "https://decantr.ai/schemas/pattern.v2.json",
  "id": "pricing",
  "version": "1.0.0",
  "decantr_compat": ">=0.9.0",
  "name": "Pricing Section",
  "description": "Pricing tier cards with plan comparison, billing toggle, and CTA buttons",
  "dependencies": {
    "patterns": {},
    "styles": {},
    "requires_core": {
      "components": [
        "Card",
        "Button",
        "Badge",
        "Switch",
        "icon"
      ],
      "charts": []
    }
  },
  "components": [
    "Card",
    "Button",
    "Badge",
    "Switch",
    "icon"
  ],
  "default_preset": "standard",
  "presets": {
    "standard": {
      "description": "Three pricing tier cards in a row with monthly/annual toggle and feature checklists",
      "components": [
        "Card",
        "Button",
        "Badge",
        "Switch",
        "icon"
      ],
      "layout": {
        "layout": "stack",
        "atoms": "_flex _col _aic _gap8 _py16 _px6",
        "slots": {
          "heading": "Section title with _heading2 _textCenter and optional subtext",
          "toggle": "Monthly/Annual billing toggle with pill-style active indicator",
          "tiers": "Horizontal row of tier Cards: _grid _gc1 _md:gc3 _gap4 _aic",
          "tier-card": "Individual Card: plan name (_heading4), price (_heading1 _fontmono), billing period, feature checklist, CTA Button",
          "feature-list": "Checklist of features with checkmark icons and _textsm",
          "cta": "Full-width Button per tier — primary variant for recommended, ghost for others"
        }
      }
    },
    "compact": {
      "description": "Simplified pricing without feature comparison, just name, price, and CTA",
      "components": [
        "Card",
        "Button",
        "Badge"
      ],
      "layout": {
        "layout": "grid",
        "atoms": "_grid _gc1 _md:gc3 _gap4 _py12 _px6",
        "slots": {
          "tier-card": "Compact Card with plan name, price, and CTA Button",
          "cta": "Primary or ghost Button"
        }
      }
    }
  },
  "visual_brief": "Three pricing tier cards in a responsive row, centered on the page. The recommended tier (typically the middle card) is visually elevated with a highlighted border in primary color, slightly larger scale (scale 1.02), and a 'Popular' or 'Recommended' badge at the top corner. Each card shows: the plan name as a heading4, the price in large monospace text (heading1 scale) with the billing period in small muted text below, a feature checklist with green checkmark icons for included features and muted X icons for excluded ones, and a full-width CTA button at the card bottom. The recommended tier uses the primary filled button variant; other tiers use ghost. A monthly/annual billing toggle is centered above the cards with a pill-style active state and the annual option showing a small 'Save 20%' badge. The section has generous vertical padding and a centered heading above everything.",
  "responsive": {
    "mobile": "Tier cards stack vertically in a single column. Recommended tier stays visually elevated but at full width. Toggle stays centered. Alternatively, tiers display as a horizontal carousel with snap-scrolling and dot indicators below. CTA buttons go full-width.",
    "tablet": "Three columns at reduced gap. Cards may be slightly narrower. Toggle and heading stay centered. Feature lists may truncate to top 5 items with 'See all features' link.",
    "desktop": "Full three-column row with comfortable gap6 spacing. Recommended tier elevated. All features visible. Toggle and heading centered above. Section padding at py16."
  },
  "motion": {
    "micro": "Tier cards lift on hover with translateY(-4px) and increased shadow over 200ms. CTA buttons brighten on hover. Toggle pill slides between states with 250ms ease.",
    "transitions": "Cards fade in and slide up with staggered delay (100ms per card) on section enter. Price value animates (counter-style) when toggling between monthly and annual. Badge pulses once on toggle change."
  },
  "default_layout": {
    "layout": "stack",
    "atoms": "_flex _col _aic _gap8 _py16 _px6",
    "slots": {
      "heading": "Section title",
      "toggle": "Billing period toggle",
      "tiers": "Pricing tier cards",
      "cta": "Action buttons"
    }
  },
  "io": {
    "consumes": [
      "billing-cycle"
    ],
    "produces": [
      "plan-selection"
    ],
    "actions": [
      "select-plan",
      "toggle-billing"
    ]
  },
  "composition": {
    "PricingSection": "Container(d-section, centered) > [Heading(heading2) + BillingToggle(d-control, pill) + TierGrid > PricingTier[]]",
    "BillingToggle": "Toggle(d-control) > [MonthlyLabel + Switch + AnnualLabel + SaveBadge?(d-annotation)]",
    "PricingTier": "Card(d-surface, elevated?: tier.recommended) > [PlanName(heading4) + Badge?(d-annotation, show: tier.recommended) + Price(mono-data, heading1) + Period(text-muted) + FeatureList > CheckItem(icon: check)[] + CTAButton(d-interactive, variant: tier.recommended ? primary : ghost, full-width)]"
  }
}
