{
  "$schema": "https://decantr.ai/schemas/pattern.v2.json",
  "id": "api-key-row",
  "version": "1.0.0",
  "decantr_compat": ">=0.9.0",
  "name": "API Key Row",
  "description": "API key display row with masked key value, name, scopes, creation date, last-used timestamp, copy-to-clipboard, and revoke actions.",
  "dependencies": {
    "patterns": {},
    "styles": {},
    "requires_core": {
      "components": [
        "Badge",
        "Button",
        "icon"
      ],
      "charts": []
    }
  },
  "contained": false,
  "components": [
    "Badge",
    "Button",
    "icon"
  ],
  "default_preset": "standard",
  "presets": {
    "standard": {
      "description": "Full API key row with name, masked key, scope badges, dates, and copy/revoke actions.",
      "components": [
        "Badge",
        "Button",
        "icon"
      ],
      "layout": {
        "layout": "row",
        "atoms": "_flex _aic _gap4 _px4 _py4 _borderb _bordermuted/10",
        "slots": {
          "key-icon": "Key icon indicator on the left",
          "key-info": "Key name (_textsm _fontmedium) and masked key value (_textxs _mono _fgmuted) stacked vertically",
          "scopes": "Horizontal row of scope Badge elements (read, write, org:read, org:write)",
          "dates": "Created and last-used timestamps",
          "actions": "Copy key Button and Revoke Button (destructive variant)"
        }
      }
    },
    "card": {
      "description": "Card-style API key display for mobile-friendly layouts. Stacked vertically.",
      "components": [
        "Badge",
        "Button",
        "icon"
      ],
      "layout": {
        "layout": "stack",
        "atoms": "_flex _col _gap3 _p4 _border _bordermuted/20 _r3",
        "slots": {
          "header": "Key name and masked value",
          "scopes": "Scope badges row",
          "meta": "Created and last-used dates",
          "actions": "Copy and revoke buttons, full width"
        }
      }
    }
  },
  "default_layout": {
    "layout": "row",
    "atoms": "_flex _aic _gap4 _px4 _py4 _borderb _bordermuted/10",
    "slots": {
      "key-icon": "Key icon",
      "key-info": "Name and masked key",
      "scopes": "Scope badges",
      "dates": "Created and last-used",
      "actions": "Copy and revoke buttons"
    }
  },
  "visual_brief": "Single horizontal row with a key icon on the far left, followed by the key name in medium-weight text and a masked key value (e.g. 'sk-****...3f2a') in monospace below it. Scope badges (read, write) appear as small colored pills in the middle. Created and last-used dates render in muted small text. Copy and revoke action buttons sit on the far right, with revoke styled as a destructive variant. Rows are separated by bottom borders. The card preset stacks everything vertically inside a bordered card for mobile use.",
  "responsive": {
    "mobile": "Switches to card preset — stacked vertical layout with full-width copy/revoke buttons. Scope badges wrap to a second line. Dates appear below key info.",
    "tablet": "Standard row layout maintained. Action buttons use icon-only mode to save horizontal space.",
    "desktop": "Full row layout with all elements visible inline. Generous horizontal spacing between key info, scopes, dates, and actions."
  },
  "layout_hints": {
    "row_layout": "Key icon (16px, muted) + key name (font-semibold) + masked value (mono-data, ••••••••) + copy button (ghost) + delete button (ghost, danger on hover). Last used timestamp on far right.",
    "reveal": "Click masked value to reveal for 5 seconds, then re-mask. Copy button copies full key.",
    "create_form": "Inline form at top: text input for key name + Create button (primary). Appears on 'New API Key' click."
  },
  "composition": {
    "ApiKeyRow": "Row(d-data-row, hoverable) > [KeyIcon + KeyInfo + ScopeBadges + DateInfo + Actions]",
    "KeyInfo": "Stack(flex-col) > [KeyName(font-medium) + MaskedKey(mono-data, text-muted)]",
    "ScopeBadges": "Row(gap-2) > Badge(d-annotation)[]",
    "Actions": "Row(d-interactive) > [CopyButton(variant: ghost) + RevokeButton(variant: destructive)]"
  }
}
