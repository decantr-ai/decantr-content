{
  "$schema": "https://decantr.ai/schemas/pattern.v2.json",
  "id": "account-settings",
  "version": "1.1.0",
  "decantr_compat": ">=1.0.0",
  "name": "Account Settings",
  "description": "Account management workspace with responsive settings navigation, grouped forms, and stable save/action zones for profile, security, preferences, and danger-zone flows.",
  "dependencies": {
    "patterns": {
      "session-manager": "^1.0.0",
      "settings-nav": "^1.0.0"
    },
    "styles": {},
    "requires_core": {
      "components": [
        "Button",
        "Avatar",
        "Badge",
        "icon"
      ],
      "charts": []
    }
  },
  "components": [
    "Button",
    "Avatar",
    "Badge",
    "icon"
  ],
  "default_preset": "profile",
  "presets": {
    "profile": {
      "description": "Profile editing with settings navigation, avatar upload, name, email, bio, and a stable save zone.",
      "components": [
        "Button",
        "Avatar",
        "icon"
      ],
      "layout": {
        "layout": "stack",
        "atoms": "_flex _col _gap8",
        "slots": {
          "nav": "Responsive settings navigation using vertical tabs on larger widths and horizontal tabs on small screens",
          "avatar": "Avatar with upload/change button",
          "form": "Name, email, bio inputs grouped in a content panel",
          "save": "Save changes button in a stable action zone"
        }
      }
    },
    "security": {
      "description": "Security settings with password change and MFA controls",
      "components": [
        "Button",
        "Badge",
        "icon"
      ],
      "layout": {
        "layout": "stack",
        "atoms": "_flex _col _gap8",
        "slots": {
          "password": "Change password form",
          "mfa": "MFA enable/disable toggle with status",
          "sessions": "Session manager component"
        }
      }
    },
    "preferences": {
      "description": "User preferences for theme, notifications, language",
      "components": [
        "Button",
        "icon"
      ],
      "layout": {
        "layout": "stack",
        "atoms": "_flex _col _gap8",
        "slots": {
          "theme": "Theme selector (light/dark/auto)",
          "notifications": "Notification toggles",
          "language": "Language/locale selector"
        }
      }
    },
    "danger-zone": {
      "description": "Account deletion with typed confirmation",
      "components": [
        "Button",
        "icon"
      ],
      "layout": {
        "layout": "stack",
        "atoms": "_flex _col _gap6",
        "slots": {
          "warning": "Deletion warning message",
          "confirm": "Typed confirmation input",
          "delete": "Delete account button"
        }
      }
    }
  },
  "layout_hints": {
    "active_state": "Active nav/tab item should have a visible indicator: accent-colored left border (for vertical nav) or bottom border (for horizontal tabs), plus accent text color.",
    "nav_position": "For settings pages, use a vertical tab nav on the left at larger widths. On small screens, switch to a horizontal scrollable tab strip or segmented control without changing the content spacing rhythm.",
    "spacing": "Nav items have consistent padding. Active item stands out but doesn't shift layout. Content cards should share the same shell inset rhythm as the rest of the dashboard.",
    "cta_treatment": "Primary save/update actions should sit in a stable action zone and feel distinct from secondary utility buttons such as avatar change, sign out, or cancel."
  },
  "visual_brief": "Settings page with vertical navigation tabs on the left (Profile, Security, Notifications, Danger Zone) and form content on the right. Active tab highlighted with an accent border. On small screens, the nav becomes a horizontal scrollable strip while the forms keep the same comfortable shell spacing. Each settings section is a d-surface card with grouped form fields, section heading, and a stable save action zone.",
  "default_layout": {
    "layout": "stack",
    "atoms": "_flex _col _gap8",
    "slots": {
      "form": "Settings form fields",
      "save": "Save button"
    }
  },
  "composition": {
    "AccountSettings": "Container(d-section, split) > [SettingsNav + SettingsContent]",
    "SettingsNav": "SettingsNavPattern(vertical-tabs, responsive)",
    "SettingsContent": "Panel(d-surface, flex-col, gap-8) > [SectionHeading + AvatarUpload? + FormFields + ActionZone]"
  }
}
