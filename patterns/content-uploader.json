{
  "$schema": "https://decantr.ai/schemas/pattern.v2.json",
  "id": "content-uploader",
  "version": "1.0.0",
  "decantr_compat": ">=1.0.0",
  "name": "Content Uploader",
  "description": "Drag-drop media upload component with file preview, progress tracking, and multi-file support. Handles images, videos, audio, and documents.",
  "dependencies": {
    "patterns": {},
    "styles": {},
    "requires_core": {
      "components": [
        "Button",
        "Card",
        "Progress",
        "icon"
      ],
      "charts": []
    }
  },
  "components": [
    "Button",
    "Card",
    "Progress",
    "icon"
  ],
  "default_preset": "standard",
  "presets": {
    "standard": {
      "description": "Multi-file upload with preview grid and progress tracking",
      "components": [
        "Button",
        "Card",
        "Progress",
        "icon"
      ],
      "layout": {
        "layout": "stack",
        "atoms": "_flex _col _gap4",
        "slots": {
          "dropzone": "Drag-drop target area with visual feedback",
          "file-list": "Uploaded files with progress bars",
          "preview": "Media preview thumbnails grid",
          "actions": "Upload, cancel, clear buttons"
        }
      }
    },
    "single": {
      "description": "Single file upload with large preview area",
      "components": [
        "Button",
        "Card",
        "icon"
      ],
      "layout": {
        "layout": "stack",
        "atoms": "_flex _col _gap4",
        "slots": {
          "dropzone": "Large drag-drop target",
          "preview": "Full-size media preview",
          "actions": "Replace, remove buttons"
        }
      }
    },
    "audio": {
      "description": "Audio upload with waveform preview",
      "components": [
        "Button",
        "Card",
        "icon"
      ],
      "layout": {
        "layout": "stack",
        "atoms": "_flex _col _gap4",
        "slots": {
          "dropzone": "Audio file drop area",
          "waveform": "Audio waveform visualization",
          "player": "Audio player controls",
          "actions": "Upload actions"
        }
      }
    }
  },
  "default_layout": {
    "layout": "stack",
    "atoms": "_flex _col _gap4",
    "slots": {
      "dropzone": "Drag-drop target area",
      "file-list": "Uploaded files list",
      "preview": "Media preview",
      "actions": "Action buttons"
    }
  },
  "io": {
    "produces": [
      "uploaded-files",
      "upload-progress"
    ],
    "consumes": []
  },
  "visual_brief": "Large dashed-border drop zone with a cloud upload icon centered above 'Drag and drop files here or click to browse' text. Active drag-over state highlights the zone with a primary color border. Below the drop zone, an uploaded files list shows each file as a row with thumbnail preview, filename, file size, a progress bar during upload, and a remove button. The progress bar fills with primary color. The single preset shows one file at a time with a larger preview. Audio preset includes waveform visualization for audio files.",
  "responsive": {
    "mobile": "Drop zone reduces in height. File list shows smaller thumbnails. Upload uses native file picker on tap. Progress bars are full-width.",
    "tablet": "Standard drop zone size. File list shows medium thumbnails with inline progress.",
    "desktop": "Large drop zone with generous padding. File list shows large thumbnails and detailed file info."
  },
  "composition": {
    "ContentUploader": "Container(d-section, flex-col, gap-4) > [Dropzone + FileList + Actions]",
    "Dropzone": "Zone(d-surface, dashed-border, drop-target) > [CloudIcon + InstructionText + BrowseButton?(d-interactive)]",
    "FileList": "List(flex-col, gap-2) > FileRow[]",
    "FileRow": "Row(d-data-row) > [Thumbnail + FileName + FileSize(text-muted) + Progress(d-data) + RemoveButton(d-interactive)]"
  }
}
